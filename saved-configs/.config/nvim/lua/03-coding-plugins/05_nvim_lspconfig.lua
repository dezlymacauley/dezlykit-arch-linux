-- The Standard nvim-lspconfig

-- If there is a project with a deno.json and a package.json
-- then no lsp will be used

return {
    -- This is a `data only` plugin so it does not have a `require` statement.
    "neovim/nvim-lspconfig"
}
